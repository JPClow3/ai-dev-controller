# Taste — Tooling preferences

- On Windows, prefers installing dependencies and tooling via winget. Confidence: 0.7
- Wants a setup script (ps1/bat) that checks dependencies, installs what's missing, and validates configs so a fresh clone "just runs". Confidence: 0.7
- Wants configs to avoid hardcoded machine-specific paths (e.g., local repo paths) so the same setup works across devices. Confidence: 0.6
- Prefers simple diagrams with minimal text for visual/outreach content (the text is supplied separately). Confidence: 0.6
- For operational dashboards in this project, prefers a TUI over a web GUI (no long-running server/frontend dependency). Confidence: 0.5
