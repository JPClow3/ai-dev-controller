# pnpm re-runs `install` before every script and treats its own build-script
# gate as a hard failure, which blocks `pnpm test`. Neither blocked package
# needs its install script here - see pnpm-workspace.yaml for why.
verify-deps-before-run=false
